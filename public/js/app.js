import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();

// Loan simulator interactivity
document.addEventListener('DOMContentLoaded', () => {
    // Add interactivity for 360° simulator if needed
});
